<a href="{{game.url}}">
    <img src="{{game.thumbnail}}" alt="{{game.name}}" class="img-responsive" />
    <h2>{{game.name}}</h2>
</a>
{{#if game.minplayers}}
    <div>Minimum players: {{game.minplayers}}</div>
{{/if}}
